# Code

No code files are duplicated in this paper package. The relevant FORTRAN trip-chain programs are staged once in `_shared_sources/mwcog-trip-chain-fortran` and referenced from `documentation/SHARED_CODE_POINTERS.md`.
