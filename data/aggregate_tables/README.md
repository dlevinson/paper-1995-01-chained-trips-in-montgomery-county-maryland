# Aggregate Tables

`original_legacy/chained_trips_tables_lotus123.wk1` is the retained Lotus 1-2-3 workbook from `ITE-CHAINS/DOS/CHAIN.WK1`. It contains the paper's aggregate Tables 1-5.

`modernized/xlsx/chained_trips_tables.xlsx` is a LibreOffice conversion of the retained workbook. `modernized/csv/chained_trips_tables_values.csv` is a values-only export for easier inspection.

`SOURCE_FILE_REVIEW.csv` records why other files in the local source folder were excluded.
